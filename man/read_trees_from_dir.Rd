% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_trees_from_dir.R
\name{read_trees_from_dir}
\alias{read_trees_from_dir}
\title{Reads Multiple Phylogenetic Trees from a Directory}
\usage{
read_trees_from_dir(dir, ext = "tre", format = c("newick", "nexus"))
}
\arguments{
\item{dir}{Character string. The path to the directory containing the tree files.}

\item{ext}{Character string. The file extension to search for (without the
leading dot). Defaults to \code{"tre"}.}

\item{format}{Character string. The file format of the trees, either \code{"newick"}
or \code{"nexus"}. Defaults to \code{"newick"}.}
}
\value{
A named list of objects of class \code{"phylo"}. Each element
is one tree. List names are the filenames the trees were read from.
}
\description{
Searches a specified directory for phylogenetic tree files with a matching
file extension and imports them into R as a list of tree objects.
It supports both Newick and NEXUS formats by wrapping the corresponding ape package functions.
The resulting list elements are automatically named using the base names of their respective
files for easy tracking.
}
\examples{
\dontrun{
  trees <- read_trees_from_dir(
    dir    = "path/to/your/trees",
    ext    = "tre",
    format = "newick"
  )
}
}
