% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rf_similarity_plot.R
\name{rf_similarity_plot}
\alias{rf_similarity_plot}
\title{Map and Plot Multidimensional Tree Similarity from Robinson-Foulds Distances}
\usage{
rf_similarity_plot(trees, method = "PH85", k = 2)
}
\arguments{
\item{trees}{An object of class \code{"multiPhylo"} containing a list of
independent phylogenetic trees sharing an identical terminal leaf-set.}

\item{method}{Character string. The distance method passed to
\code{ape::dist.topo()}. Use \code{"PH85"} for the Robinson-Foulds
symmetric difference (default).}

\item{k}{Integer. Number of dimensions for the MDS projection.
Default is \code{2} for a standard 2D scatter plot.}
}
\value{
The MDS coordinate matrix invisibly. A scatter plot is drawn
on the current graphics device as a side effect.
}
\description{
Calculates the symmetric topological Robinson-Foulds (RF) distance between pairs
of input trees, projects their structural proximity patterns onto a lower-dimensional
continuous plane using Classical Multidimensional Scaling (MDS), and renders a
labeled ordination scatter plot on the current graphics device.
}
\examples{
\dontrun{

# Generate a list of 5 mock random trees with identical tip labels
set.seed(42)
mock_trees <- replicate(5, ape::rtree(10, tip.label = paste0("Taxon_", 1:10)),
  simplify = FALSE
)
class(mock_trees) <- "multiPhylo"
names(mock_trees) <- paste0("Analysis_", 1:5)

# Create the plots/ directory if missing and run the visualization pipeline
rf_similarity_plot(mock_trees)
}
}
